#!/bin/bash

## hyphop ##

. ./,config 

$test $feeds update -i

action=$1
[ "$action" = "" ] && action=install

for f in `grep -v \# $0.list`; do
    echo "[i] $action feed $f"
    $test $feeds $action $f
done

